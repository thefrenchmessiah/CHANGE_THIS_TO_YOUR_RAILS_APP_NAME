<%= simple_form_for[@emotion @booking] do |f| %>

  <%# <div class="field"> %>
    <%# <%= f.hidden_field :client_id, value: current_user.id %>
  <%# </div> %>

  <div class="field">
    <%= f.hidden_field :owner_id, value: @emotion.user.id %>
  </div>

  <div class="field">
    <%= f.label :start_date %>
    <%= f.date_field :start_date %>
  </div>

  <div class="field">
    <%= f.label :end_date %>
    <%= f.date_field :end_date %>
  </div>

  <div class="actions">
    <%= f.submit "Book Emotion" %>
  </div>
<% end %>
