<%= form_with(model: @emotion, url: emotions_path, method: :post) do |form| %>
  <% if @emotion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@emotion.errors.count, "error") %> prohibited this emotion from being saved:</h2>

      <ul>
        <% @emotion.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
