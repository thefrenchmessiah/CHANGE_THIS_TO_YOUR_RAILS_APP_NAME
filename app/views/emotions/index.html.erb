<div class="banner-home">
  <h1>Welcome to</h1>
  <img src="https://res.cloudinary.com/dmilm0nih/image/upload/v1700755879/d04tewomqjyjzf2esheu.png" alt="intensify logo" style="height: 200px;">
</div>
<div class="center-text">
  <br>
  <h2 class="h2-pink margin-top-20">What emotion would you like to feel today?</h2>
</div>
<div class="circle-container">
  <%= link_to emotions_path(filter: 1), class: "category-avatar" do %>
    😃
  <% end %>
  <%= link_to emotions_path(filter: 2), class: "category-avatar" do %>
    😢
  <% end %>
    <%= link_to emotions_path(filter: 3), class: "category-avatar" do %>
    😡
  <% end %>
      <%= link_to emotions_path(filter: 4), class: "category-avatar" do %>
    😰
  <% end %>
      <%= link_to emotions_path(filter: 5), class: "category-avatar" do %>
    🤮
  <% end %>
</div>
<br>
<div class="cards-container">
  <div class="card-grid">
    <% @emotions.each do |emotion| %>
      <div class="card">
        <% if emotion.photo.attached? %>
          <%= cl_image_tag emotion.photo.key, alt: emotion.name, class: 'card-image' %>
        <% end %>
        <div class="card-info">
          <h2 class="emotion-name"><%= emotion.name %></h2>
          <p class="emotion-description"><%= emotion.description %></p>
          <%= link_to 'Book', emotion_path(emotion), class: 'button-orange' %>
        </div>
      </div>
    <% end %>
  </div>
</div>
